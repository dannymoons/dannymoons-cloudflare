/** Future Payload mapping: philosophyStatement. */
export function Philosophy() {
	return (
		<section className='border-[var(--line)] border-t px-5 py-20 sm:px-8 sm:py-28'>
			<div className='mx-auto max-w-6xl'>
				<span className='at-reveal mb-10 block text-xs uppercase tracking-[0.3em] [color:var(--gold)]'>
					Philosophy
				</span>
				<p className='at-reveal max-w-4xl font-[family-name:var(--font-display)] font-bold text-[clamp(1.75rem,4.5vw,3.5rem)] leading-[1.12] tracking-[-0.02em]'>
					Space is not empty. It is the medium through which life becomes
					legible — and every line we draw is an argument for how people should
					inhabit the world.
				</p>
			</div>
		</section>
	)
}
