/** Future Payload mapping: bigStatement. */
export function Mission() {
	return (
		<section
			id='mission'
			className='px-5 py-20 [background:var(--sky)]/40 sm:px-8 sm:py-28'
		>
			<p className='bl-reveal mx-auto max-w-4xl text-center font-[family-name:var(--font-display)] font-semibold text-[clamp(1.5rem,4vw,2.5rem)] leading-[1.3] [color:var(--ink)]'>
				Hospital walls shouldn&apos;t feel cold. Bloom funds spaces where
				children can{' '}
				<span className='[color:var(--petal)]'>laugh, rest, and heal</span> —
				surrounded by colour, nature, and the people who love them.
			</p>
		</section>
	)
}
