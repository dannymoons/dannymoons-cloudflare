/** Future Payload mapping: bigStatement. */
export function Ethos() {
	return (
		<section className='border-[var(--line)] border-y px-5 py-20 sm:px-8 sm:py-28'>
			<div className='so-reveal mx-auto max-w-4xl text-center'>
				<span className='text-xs uppercase tracking-[0.28em] [color:var(--gold)]'>
					Philosophy
				</span>
				<blockquote className='mt-8 font-[family-name:var(--font-display)] font-light text-[clamp(1.75rem,5vw,3.25rem)] italic leading-[1.25] [color:var(--cream)]'>
					We do not chase trends. We follow the light — from the first asparagus
					of spring to the last fig of autumn, every plate is a moment the
					season will not offer twice.
				</blockquote>
				<p className='mx-auto mt-8 max-w-xl text-sm leading-relaxed [color:var(--mute)] sm:text-base'>
					Solstice is a collective of chefs, foragers, and sommeliers united by
					a single conviction: that the finest dining is not entertainment, but
					communion with time itself.
				</p>
			</div>
		</section>
	)
}
