/** Future Payload mapping: bigStatement. */
export function Manifesto() {
	return (
		<section className='border-[var(--line)] border-y px-5 py-20 sm:px-8 sm:py-28'>
			<blockquote className='of-reveal mx-auto max-w-4xl text-center font-[family-name:var(--font-display)] text-[clamp(1.5rem,4vw,2.75rem)] leading-[1.3]'>
				&ldquo;The internet emits more CO₂ than aviation. Every megabyte
				transferred, every autoplaying video, every bloated framework is a{' '}
				<span className='italic [color:var(--forest)]'>
					choice we can unmake
				</span>
				&nbsp;&mdash; one lean page at a time.&rdquo;
			</blockquote>
			<p className='of-reveal mx-auto mt-8 max-w-md text-center text-xs uppercase tracking-[0.24em] [color:var(--mute)]'>
				The OFFSET manifesto
			</p>
		</section>
	)
}
